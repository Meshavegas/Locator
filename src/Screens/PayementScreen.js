import { View, Text } from "react-native";
import React from "react";

const PayementScreen = () => {
  return (
    <View>
      <Text>PayementScreen</Text>
    </View>
  );
};

export default PayementScreen;
